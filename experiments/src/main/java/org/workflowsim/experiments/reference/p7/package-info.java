/**
 * P7 冻结 reference 基线的矩阵、执行入口和显式输入身份契约。
 *
 * <p>本包不属于核心模拟器公共算法 API。它只在 {@code -Pexperiments} 下构建，要求调用者
 * 显式提供绝对 {@code datasets} 根目录和空输出目录，并将自身 driver、协议与输入根写入
 * v3 evidence bundle。P7 的实验边界和不可主张范围以项目文档为准。</p>
 */
package org.workflowsim.experiments.reference.p7;
